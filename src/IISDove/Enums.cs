namespace IISDove
{
    public enum ResponseType {
        Successful = 1,
        Failed = 0
    }

    public enum StatusCode {
        None = 0,
        Ok = 1,
        SiteOccurError = 100
    }

    public enum DoveType {
        Sender = 1,
        Commander = 2
    }

}
